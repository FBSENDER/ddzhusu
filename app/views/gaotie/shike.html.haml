- title "#{@from.name}到#{@to.name}高铁时刻表_滴滴高铁"
- keywords "#{@from.name}到#{@to.name}高铁，#{@from.name}到#{@to.name}高铁时刻表"
- head_desc "#{@from.name}到#{@to.name}高铁动车时刻表、火车票、车次等相关查询，滴滴住宿尽力为您提供这些信息。"

%header
  %p= "滴滴住宿，尽力为您提供#{@from.name}到#{@to.name}高铁动车时刻表、火车票、车次等相关信息~"
%section
  %h1= "#{@from.name}到#{@to.name}高铁"
  %p= "关于#{@from.name}到#{@to.name}火车车次列余票信息查询工作，遥遥无期~"
%section 
  %h2= "#{@from.name}至#{@to.name}动车时刻表"
  %p= "#{@from.name}-#{@to.name}高铁今日共有#{@numbers.size}次，下面是其具体信息。"
  - @numbers.each do |num|
    %div
      %h3= num.name
      - JSON.parse(num.infos).each_with_index do |data, index|
        %p= "站次：#{index + 1}，到达时间：#{data["arrivalTime"]}，发车时间：#{data["departTime"]}，停留时间：#{data["stopTime"]}"
%section
  %h2 热门出行高铁动车组车次信息
  - @related_urls.each do |url|
    %a{href: "/train/timetable/#{url.values.first}/gaotie.html"}= url.keys.first
%section
  %h2= "#{@from.name}到#{@to.name}火车时刻表"
  %a{href: "/train/timetable/#{@from.pinyin}-#{@to.pinyin}/"}= "#{@from.name}到#{@to.name}火车"
%footer
  %p 更多更详细的火车车次信息查询，百度一下，你准知道~
